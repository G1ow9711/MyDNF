# 项目协作约定

- 能拆分成独立模块、互不阻塞的开发、测试、审查、资料检索或验收任务，优先使用多个 agent 并行执行。
- 并行 agent 的工作必须有清晰边界：不同 agent 不同时修改同一文件；如必须修改同一文件，先由主 agent 合并计划后再执行。
- agent 产出的结论必须由主 agent 汇总、复核，并通过本项目测试、构建或浏览器验收后再提交。
- Git 提交信息必须使用中文。
