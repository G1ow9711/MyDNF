# 副本定向掉落与结算反馈实施计划

1. 在 `combat.test.ts` 先增加普通/冒险/勇士掉落矩阵、副本池隔离、轮换和 `goldFind` 失败测试。
2. 在 `combat.ts` 增加小型纯函数，按房间角色决定稀有度，并按背包持有数量稳定选择候选装备。
3. 在 `app-integration.test.ts` 先增加最近结算保留、装备元数据和最终回城展示失败测试，同时覆盖 reducer 与最终 HTML。
4. 在 `app.ts` 接入 `lastLoot`、目录元数据解析和全局结算条；在 `styles.css` 添加不遮挡战斗主体的紧凑表现。
5. 增加真实浏览器勇士首房键盘验收，验证史诗副本池、结算 DOM、自动存档和刷新恢复。
6. 依次运行聚焦测试、核心串行回归、生产构建、真实浏览器聚焦与全套串行验收。
7. 更新三份项目记录，使用中文提交信息提交并推送当前分支。
