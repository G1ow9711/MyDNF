# 双击方向奔跑实施计划

1. 在浏览器键盘路由中增加可释放的 280ms 同方向二次按下状态，不修改战斗内核。
2. 将双击来源合并到每 48ms 的权威 `combatTick` 冲刺输入，并在重渲染后暴露来源状态。
3. 给图集奔跑帧增加双击来源扬尘/速度线，更新键位提示。
4. 新增真实浏览器路线：单击不跑、双击保持奔跑、X 触发 `dash-light`、松键退出。
5. 跑完整回归、生产构建、中文提交和推送。
