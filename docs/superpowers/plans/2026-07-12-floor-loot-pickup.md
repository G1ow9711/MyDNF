# 地下城地面掉落与拾取实施计划

1. 为 `CombatRun` 增加地面掉落实体并导出生成、拾取函数。
2. 修改 `finishRoom`，已拾取奖励不重复，未拾取奖励自动收取。
3. 修改应用 Reducer，区分“同房拾取”和“房间完成”两种新 loot event。
4. 渲染世界空间掉落光柱、装备核心和稳定 `data-*`。
5. 增加核心、应用、UI 和真实键盘验收，运行全量回归后中文提交推送。
