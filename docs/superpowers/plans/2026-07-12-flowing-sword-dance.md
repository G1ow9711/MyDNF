# 流光剑阵舞实施计划

1. 扩展 `CombatHitPhase` 和 `CombatVfxCue`，加入左右舞步阶段。
2. 将 `flowingLightChainStages` 改为七段真实命中时间轴，并同步技能时长、移动终点和终结控制。
3. 扩展帧动画映射，使五种阶段使用不同动作帧范围。
4. 为左右舞步加入独立人物、武器、剑轨、残影、施法和目标命中特效动画。
5. 更新核心、集成、样式与真实浏览器测试，先观察旧三段实现的 RED，再完成 GREEN。
6. 运行全量非浏览器回归、构建和真实浏览器验收，检查截图后中文提交并推送。
