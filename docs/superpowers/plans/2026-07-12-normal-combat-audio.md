# 普通战斗接触帧音效实施计划

1. 为输入挥动、位移和七类普通命中建立独立程序化播放方案。
2. 将战斗事件音效映射从剑舞专用函数扩展为普通攻击与剑舞共享的命中派生器。
3. 替换 `combatAction` 的通用 `hit-light` 输入音，避免跳跃和后跳发出打击声。
4. 增加应用、播放计划和真实浏览器 RED/GREEN 覆盖。
5. 运行全量回归、构建、页面检查，并使用中文提交推送。
