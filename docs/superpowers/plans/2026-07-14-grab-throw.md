# Task 212 Grab And Throw Plan

1. Add failing combat tests for trash catch/throw, same-target release, knockdown, immunity, no resistance teleport, and hitstop timer shifting.
2. Add failing mounted UI, computed-style, audio, and real-keyboard acceptance tests.
3. Extend combat state and scheduled hit effects with authoritative grab phases and one shared eligibility rule.
4. Rebuild Iron Palm as catch plus throw while preserving cooldown, resource, input, and class identity.
5. Add synchronized frame-atlas/fallback model motion, grab VFX, damage feedback, and authored audio.
6. Run focused and full regressions, verify in real browser with real keys, inspect screenshot, commit and push with Chinese message.
